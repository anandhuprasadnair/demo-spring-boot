pipeline{
    
    agent any

    stages{
        stage("build"){
            steps{
                echo "Build step"
            }
        }

        stage("push"){
            steps{
                echo "push step"
            }
        }

        stage("deploy"){
            steps{
                echo "deploy step"
            }
        }
    }
}